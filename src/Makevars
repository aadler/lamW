PKG_FFLAGS = $(SHLIB_OPENMP_FFLAGS)
PKG_LIBS = $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS) $(SHLIB_OPENMP_CFLAGS)

C_OBJS = utils_and_wrappers.o
FT_OBJS = lambertW.o
OBJECTS = $(FT_OBJS) $(C_OBJS)

all:
	@$(MAKE) $(SHLIB)
	@rm -f *.mod *.o

$(SHLIB): $(OBJECTS)

cleanup:
		@rm -rf *.mod *.o
